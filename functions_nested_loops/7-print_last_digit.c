#include "main.h"
/**
 *print_lastdigit: print the last digit of a number
 *
 *Return: always return 0
 *
 */

int print_last_digit(int)

{
  
  if (r >= 0)
    
    {
      printf("%d\n", lastdigit);
      
    }
return (lastdigit);
}
