#include "main.h"
/**
 *
 *
 *
 *
 */
void print_to_98(int n);

int a = 0;
int b = 0;

for (a = 0; a <= 0; a++)
  {
    for (b = 0; b <= 0; b++)
      {
        _putchar((a / 10) + '0');
	_putchar((a % 10) + '0');
	_putchar(' ');

	_putchar((b / 10) + '0');
	_putchar((b % 10) + '0');
	_putchar(' ')