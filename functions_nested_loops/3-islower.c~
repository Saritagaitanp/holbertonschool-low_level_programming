#include "main.h"
/**
 * Return: always print zero
 *_islower - print Write a function that checks for lowercase character.
 * @c: dentro de este esta el alphabet
 */
int _islower(int c)
{
if (c <= 'z' && c >= 'a')
{
return (1);
}
else
{
return (0);
}
}
