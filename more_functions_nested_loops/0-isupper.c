#include "main.h"
/**
 * _isupper - check the caracter
 * @c: input
 * Return: 1 if c is uppercase, 0 if not
 */

int _isupper(int c)

{

if ((c >= 'a') && (c <= 'z'))

return (0);
else

return (1);
}
